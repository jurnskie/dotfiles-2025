#!/bin/bash
{{ range .projects }}
# Stel de variabele in voor Bash
export PROJECT_NAME="{{ . }}"

mkdir -p ~/code/${PROJECT_NAME}

# Genereer de .env - we wijzen nu naar het juiste pad
cat <<EOF > ~/code/${PROJECT_NAME}/.env
{{ include ".chezmoitemplates/statamic_env" }}
EOF

{{ end }}
